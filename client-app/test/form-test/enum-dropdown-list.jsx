/**
 * React-based enum drop-down list.
 */
'use strict';

let React = window.React;

export let EnumDropDownList = React.createClass({
    render() {
        return (
            <select>
                <option>Option 1</option>
            </select>
        );
    }
});
